version: "2.0"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: ./DockerfileDev
    restart: always
    env_file: ./frontend/.env
    volumes:
      - ./frontend/src/:/app/
      - /app/node_modules/
    ports:
      - 3000:3000
  
  backend:
    build:
      context: ./backend
      dockerfile: ./DockerfileDev
    restart: always
    env_file: ./backend/.env
    volumes:
      - ./backend/src/:/app/
    ports:
      - 9000:9000